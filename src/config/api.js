const baseURL = "https://api.spacexdata.com/v3/";

export const allLaunchesAPI = `${baseURL}launches`;
export const pastLaunchesAPI = `${baseURL}launches/past`;
export const upcomingLaunchesAPI = `${baseURL}launches/upcoming`;
